# Плоские структуры: массивы, списки, хеш-таблицы, кэши

**Предпосылки:** читатель понимает базовые понятия программирования (переменные, типы данных, условия, циклы, функции), указатели/ссылки и оценку сложности в O(…).

В этих структурах почти всё сводится к двум вопросам:
1. По чему ищем элемент: по позиции (индекс) или по ключу?
2. Чем платим за скорость: сдвигами и копированием, лишними указателями, промахами кэша CPU, блокировками?

Типичный путь выбора выглядит так: массив → динамический массив → связный список → ограниченные интерфейсы (stack/queue/deque) → хеш‑таблица → кэши с вытеснением (LRU/LFU/clock‑sweep).

---

## Абстрактный тип данных (ADT)

**ADT — Abstract Data Type.** Спецификация структуры данных через набор операций и их поведение, без указания реализации.

**Этимология:** "Abstract" — от латинского *abstrahere* ("отвлекать, отделять"). Отделяем *что* структура делает от *как* устроена.

ADT описывает контракт: какие операции доступны и с какой сложностью. Реализация может быть любой, если контракт соблюдён. Одну и ту же задачу можно решить разными структурами — ADT позволяет сравнивать их по единому интерфейсу.

---

## Задача: хранить N элементов с доступом по номеру

Самая базовая потребность — положить несколько значений и достать любое из них по номеру за константное время.

### ADT: IndexedCollection

| Операция | Сигнатура | Сложность |
|----------|-----------|-----------|
| Получить элемент | `get(i) → value` | O(1) |
| Записать элемент | `set(i, value)` | O(1) |
| Узнать размер | `size() → n` | O(1) |

**Инвариант:** допустимые индексы от 0 до n-1. Размер фиксирован при создании.

### Реализация: Массив (Array)

**Этимология:** "Array" (англ.) — буквально "ряд", "строй", "расстановка". Военный термин: солдаты выстроены в ряд, плечом к плечу, без промежутков.

**Определение:** структура данных, которая хранит фиксированное количество элементов одного типа в непрерывном блоке памяти.

Ключевые свойства:
- **Фиксированное количество** — размер задаётся при создании
- **Одного типа** — все элементы занимают одинаковое количество байт
- **Непрерывный блок** — между элементами нет "дыр"
- **Доступ по индексу за O(1)** — следствие трёх предыдущих свойств

### Почему O(1) — вывод, а не магия

Пусть массив начинается с адреса `base`, каждый элемент занимает `size` байт. Тогда элемент с индексом `i` лежит по адресу:

```
address(i) = base + i × size
```

Одно умножение, одно сложение — количество операций не зависит от `n`. Отсюда O(1).

**Важно:** формула работает только потому, что все элементы одного размера. Если элементы разного размера, формула ломается.

### Представление в памяти

```
┌─────────────────────────────────────────────────────┐
│  base                                               │
│   ↓                                                 │
│  [elem 0][elem 1][elem 2][elem 3]...[elem n-1]      │
│   ←size→ ←size→  ←size→                             │
└─────────────────────────────────────────────────────┘
```

Три параметра определяют массив:
- `base` — адрес начала
- `size` — размер одного элемента в байтах
- `n` (или `length`) — количество элементов

### Инварианты массива

1. **Непрерывность:** элементы занимают адреса от `base` до `base + n × size - 1` без пропусков
2. **Однородность:** каждый элемент занимает ровно `size` байт
3. **Границы:** допустимые индексы — от `0` до `n-1` включительно

Нарушение инварианта 3 — **выход за границы массива**. В C это неопределённое поведение. В языках с проверкой границ (Rust/Java/Python) это паника или исключение. В Ruby поведение зависит от операции: `arr[i]` обычно возвращает `nil`, а `arr.fetch(i)` поднимает `IndexError`.

### Операции и сложность

| Операция | Время | Почему |
|----------|-------|--------|
| Чтение по индексу | O(1) | Формула адреса |
| Запись по индексу | O(1) | Формула адреса |
| Поиск по значению | O(n) | Нужно проверить каждый элемент |
| Вставка в позицию i | O(n) | Сдвиг элементов |
| Удаление из позиции i | O(n) | Сдвиг элементов |

Сдвиг — цена за непрерывность: чтобы сохранить порядок, элементы после места операции приходится перемещать.

### Что если элементы разного размера?

Два решения:

**1. Padding (выравнивание)** — добавляем пустые байты, чтобы все элементы занимали одинаковое место. Так работают структуры в C. Платим памятью.

**2. Массив указателей** — сам массив хранит указатели одинакового размера, а данные лежат отдельно в куче. Так устроены массивы в Ruby, Python, Java. Формула работает для указателей, но теряем **локальность данных**.

**Примечание об указателях:** указатель — это адрес в памяти, не обязательно в куче. Может указывать на стек, статическую память, куда угодно. "Куча" здесь — типичный случай для динамически создаваемых объектов.

### Локальность данных

Процессор читает память **кэш-линиями** — блоками фиксированного размера (часто 64 байта; зависит от CPU). Когда читаешь один байт, обычно загружается вся линия вокруг него.

В "честном" массиве (элементы подряд) обход — последовательное чтение. Кэш работает идеально.

В массиве указателей каждый указатель ведёт в «случайное» место памяти. Каждый прыжок — потенциальный **cache miss**. Попадание в L1 обычно стоит единицы циклов, промах до RAM — сотни циклов. Поэтому «прыжки по указателям» часто доминируют над асимптотикой.

### Проблема: фиксированный размер

Массив реализует IndexedCollection, но размер фиксирован при создании. Если заранее неизвестно сколько элементов будет — массив неудобен. Выделять "с запасом" — тратить память. Выделять "впритык" — не влезет новый элемент.

---

## Задача: коллекция неизвестного размера

Хотим добавлять элементы, не зная заранее сколько их будет. При этом сохранить O(1) доступ по индексу.

### ADT: DynamicCollection

Расширяем IndexedCollection операцией добавления:

| Операция | Сигнатура | Сложность |
|----------|-----------|-----------|
| Получить элемент | `get(i) → value` | O(1) |
| Записать элемент | `set(i, value)` | O(1) |
| Добавить в конец | `append(value)` | O(1) амортизировано |
| Узнать размер | `size() → n` | O(1) |

**Отличие от IndexedCollection:** размер может расти.

### Реализация: Динамический массив (Dynamic Array)

**Этимология:** "Dynamic" — от греческого *dynamis* (сила, способность к изменению). Массив, способный менять свой размер.

**Идея:** храним обычный статический массив внутри, но отслеживаем две величины:
- `capacity` — сколько места выделено
- `length` — сколько элементов реально используется

Пока `length < capacity`, вставка в конец — O(1). Когда `length == capacity` — делаем **resize**.

### Resize: механизм

```
Было:   capacity=4, length=4
        [A][B][C][D]    — массив полон

Хотим добавить E.

Шаг 1: Выделяем новый массив (обычно × 2)
        [_][_][_][_][_][_][_][_]   — capacity=8

Шаг 2: Копируем старые элементы
        [A][B][C][D][_][_][_][_]

Шаг 3: Освобождаем старый массив

Шаг 4: Добавляем новый элемент
        [A][B][C][D][E][_][_][_]   — length=5, capacity=8
```

### Амортизированный анализ

**Амортизация** — из финансов: "растягиваем" большой разовый расход на много маленьких платежей.

После `n` вставок суммарно скопировали: `1 + 2 + 4 + 8 + ... + n/2 ≈ n` элементов.

На `n` операций потратили O(n) работы → в среднем на одну вставку O(1).

### Почему мультипликативный рост

При удвоении после resize есть `n` свободных слотов. Следующий resize через `n` вставок. Баланс расходов и доходов.

При увеличении на 1: каждая вставка вызывает resize. За `n` вставок — O(n²) суммарно.

Главное — **мультипликативный** рост (×2, ×1.5), а не аддитивный (+1, +10).

### Коэффициент роста в реализациях

Коэффициент — деталь реализации: часто это ~1.5× или 2×, но некоторые рантаймы используют формулу «overallocation» или меняют коэффициент в зависимости от размера массива. Смысл один: больший рост реже копирует, но увеличивает пик памяти; меньший рост чаще копирует, но экономит память.

### Накладные расходы памяти

В худшем случае после resize массив заполнен наполовину: до 50% памяти — пустые слоты.

Компромисс: меньший коэффициент — меньше памяти, чаще копируем; больший коэффициент — больше памяти, реже копируем.

### Проблема: O(n) вставка в начало

Доступ по индексу — O(1). Добавление в конец — O(1) амортизированно. Но вставка в начало — по-прежнему O(n): нужно сдвинуть все элементы.

Если задача — часто вставлять в начало или середину, динамический массив неэффективен.

---

## Задача: быстрая вставка в начало и середину

Хотим вставлять и удалять элементы в произвольных местах за O(1), не сдвигая остальные.

### ADT: Sequence

| Операция | Сигнатура | Сложность |
|----------|-----------|-----------|
| Вставить в позицию | `insertAt(i, value)` | O(1)* |
| Удалить из позиции | `removeAt(i) → value` | O(1)* |
| Получить элемент | `get(i) → value` | O(n) |

`*` — если уже есть указатель на место вставки

**Компромисс:** теряем O(1) доступ по индексу в обмен на O(1) вставку.

### Реализация: Связный список (Linked List)

**Этимология:** "Linked List" — буквально "связанный список". Элементы связаны указателями, образуя цепочку.

**Образ:** вагоны поезда. Каждый вагон соединён со следующим сцепкой (указателем). Чтобы добраться до вагона 500, нужно пройти через все предыдущие. Но отцепить вагон из середины — быстро, если ты уже на месте.

**Определение:** структура данных из узлов, где каждый узел содержит данные и указатель(и) на другие узлы, образуя последовательность.

Ключевые свойства:
- Элементы не обязаны лежать в памяти подряд
- Связь через указатели
- Нет формулы для адреса по индексу

### Варианты

**Односвязный (Singly Linked List):**

```
head → [A|•] → [B|•] → [C|•] → nil
```

Каждый узел знает только следующий. Движение только вперёд.

**Двусвязный (Doubly Linked List):**

```
nil ← [•|A|•] ⇄ [•|B|•] ⇄ [•|C|•] → nil
       ↑                       ↑
     head                    tail
```

Каждый узел знает и предыдущий, и следующий. Движение в обе стороны.

**Кольцевой (Circular):** последний указывает на первый.

### Структура узла

**Односвязный:**

```ruby
class Node
  attr_accessor :data, :next
end
```

**Двусвязный:**

```ruby
class Node
  attr_accessor :data, :prev, :next
end
```

### Ключевое различие: удаление узла

**Двусвязный:** O(1) — читаем `prev`, обновляем соседей.

**Односвязный:** O(n) — нужно найти предыдущий узел, а он неизвестен.

Это главная причина существования двусвязного списка: когда нужно удалять из середины, имея только указатель на узел.

### Операции и сложность

| Операция | Односвязный | Двусвязный |
|----------|-------------|------------|
| Доступ по индексу | O(n) | O(n) |
| Вставка в начало | O(1) | O(1) |
| Вставка в конец (с tail) | O(1) | O(1) |
| Вставка после известного узла | O(1) | O(1) |
| Удаление из начала | O(1) | O(1) |
| Удаление из конца | O(n) | O(1) |
| Удаление известного узла | O(n)* | O(1) |

`*` — нужен предыдущий узел

### Накладные расходы памяти

**Односвязный:** +8 байт на узел (указатель на 64-bit системе).

**Двусвязный:** +16 байт на узел.

Для C-style списка из `n` int'ов (4 байта): 12 байт на элемент вместо 4. Overhead 200% (8 байт указателя / 4 байта данных).

Плюс каждый узел — отдельная аллокация. Нагрузка на GC или много malloc/free.

### Реализация односвязного списка (Ruby)

```ruby
class Node
  attr_accessor :value, :next

  def initialize(value, node = nil)
    @value = value
    @next = node
  end
end

class List
  def initialize
    @head = nil
    @tail = nil
  end

  def prepend(value)
    new_node = Node.new(value, @head)
    @tail = new_node unless @head
    @head = new_node
  end

  def append(value)
    new_node = Node.new(value)
    if @tail
      @tail.next = new_node
      @tail = new_node
    else
      @tail = @head = new_node
    end
  end

  def remove_first
    return nil unless @head
    value = @head.value
    @head = @head.next
    @tail = nil unless @head  # критично: обнулить tail при опустошении!
    value
  end
end
```

**Типичный баг:** забыть обнулить `@tail` при удалении последнего элемента. После этого `append` сломается — будет писать через "мёртвый" указатель.

### Проблема: нет O(1) доступа по индексу, плохая локальность

Вставка в начало — O(1). Но доступ по индексу — O(n): нужно пройти по цепочке от начала.

Ещё хуже — **плохая локальность данных**. Узлы разбросаны по куче, каждый переход — потенциальный cache miss. На практике динамический массив часто быстрее даже при частых вставках из-за лучшей работы с кэшем процессора.

### Когда что выбирать

**Массив лучше:**
- Нужен произвольный доступ по индексу
- Основные операции — чтение и запись
- Важна локальность (обход всех элементов)
- Вставки/удаления в основном в конец

**Связный список лучше:**
- Частые вставки/удаления в начало
- Частые вставки/удаления в середину (если есть указатель)
- Нужно "сращивать" или "разрезать" списки за O(1)

**На практике:** динамический массив выигрывает в большинстве случаев из-за кэш-локальности.

---

## Задача: ограничить операции для гарантий

У массива и списка много операций: доступ по индексу, вставка в любое место, удаление из любого места. Иногда это избыточно — и даже вредно.

Если алгоритму нужен только LIFO-порядок (последний пришёл — первый вышел), доступ к середине — источник ошибок, не фича. Ограничение операций даёт:
- **Гарантии корректности** — код не сломает инварианты
- **Свободу реализации** — можно поменять массив на список, не ломая код
- **Документацию намерения** — читающий сразу понимает: "здесь важен LIFO"

### ADT: Stack (Стек)

**LIFO** — Last In, First Out. Последним пришёл — первым вышел.

**Образ:** стопка тарелок. Берёшь верхнюю, которую положил последней.

| Операция | Сигнатура | Сложность |
|----------|-----------|-----------|
| Положить на вершину | `push(x)` | O(1) |
| Снять с вершины | `pop() → x` | O(1) |
| Посмотреть вершину | `peek() → x` | O(1) |
| Проверить пустоту | `empty?() → bool` | O(1) |

**Реализации:**

| Реализация | push | pop | Особенности |
|------------|------|-----|-------------|
| Динамический массив | O(1)* | O(1) | Лучшая локальность |
| Односвязный список | O(1) | O(1) | Нет resize |

На практике почти всегда массив.

**Call stack** работает по LIFO: последняя вызванная функция завершается первой.

### Реализация стека на односвязном списке (Ruby)

```ruby
class Stack
  def initialize
    @head = nil
  end

  def push(value)
    @head = Node.new(value, @head)
  end

  def pop
    return nil unless @head
    value = @head.value
    @head = @head.next
    value
  end

  def peek
    @head&.value  # safe navigation: nil если пусто
  end

  def empty?
    @head.nil?
  end
end
```

**Почему работаем с head, а не tail:** в односвязном списке `pop` с tail требует предыдущий узел — O(n). С head — O(1).

### ADT: Queue (Очередь)

**FIFO** — First In, First Out. Первым пришёл — первым вышел.

**Образ:** очередь в магазине. Кто раньше встал, тот раньше обслужен.

| Операция | Сигнатура | Сложность |
|----------|-----------|-----------|
| Добавить в конец | `enqueue(x)` | O(1) |
| Забрать из начала | `dequeue() → x` | O(1) |
| Посмотреть первый | `front() → x` | O(1) |
| Проверить пустоту | `empty?() → bool` | O(1) |

**Реализации:**

| Реализация | enqueue | dequeue | Особенности |
|------------|---------|---------|-------------|
| Ring buffer | O(1)* | O(1) | Локальность |
| Односвязный список с tail | O(1) | O(1) | Проще код |

`*` — амортизированно, если буфер растёт при переполнении

#### Ring Buffer (Кольцевой буфер)

Очередь на наивном массиве неэффективна: `dequeue` оставляет "мёртвую зону" в начале. Кольцевой буфер решает это — два указателя (`head`, `tail`) ходят по кругу.

```
Физически:   [_][_][A][B][C][D][_][_]
                    ↑           ↑
                  head        tail

Переход через край: next = (current + 1) % capacity
```

Обе операции O(1). "Мёртвая зона" становится свободным местом для новых элементов.

### ADT: Deque (Дек)

**Deque** — Double-Ended Queue. Произносится как "deck" (колода карт).

| Операция | Сигнатура | Сложность |
|----------|-----------|-----------|
| Добавить в начало | `push_front(x)` | O(1) |
| Добавить в конец | `push_back(x)` | O(1) |
| Удалить из начала | `pop_front() → x` | O(1) |
| Удалить из конца | `pop_back() → x` | O(1) |

**Почему односвязный список не подходит:** `pop_back` требует предыдущий узел — O(n).

**Реализации:** ring buffer или двусвязный список.

### Сводка: три уровня свободы

| ADT | Добавление | Удаление | Минимальная структура |
|-----|-----------|----------|----------------------|
| Стек | один конец | тот же | массив или односвязный |
| Очередь | один конец | противоположный | ring buffer или односвязный с tail |
| Дек | оба конца | оба конца | ring buffer или двусвязный |

### Проблема: доступ только по числовому ключу

Все три ADT работают с позицией: начало, конец, индекс. Ключ — всегда число.

Что если нужен доступ по произвольному ключу? Строка "alice", объект, UUID? Нужна структура, которая превращает произвольный ключ в O(1) доступ.

---

## Задача: доступ по произвольному ключу за O(1)

Массив даёт O(1) доступ, но только по числовому индексу 0..n-1. Хотим O(1) доступ по **произвольному ключу** — строке, объекту, чему угодно.

### ADT: Dictionary (Словарь)

Коллекция пар ключ-значение:

| Операция | Сигнатура | Сложность |
|----------|-----------|-----------|
| Получить значение | `get(key) → value` | O(1) в среднем |
| Записать значение | `put(key, value)` | O(1) в среднем |
| Удалить | `delete(key)` | O(1) в среднем |
| Проверить наличие | `contains?(key) → bool` | O(1) в среднем |

**Примечание:** "в среднем" — худший случай O(n) при всех ключах в одной корзине.

### Реализация: Хеш-таблица (Hash Table)

**Этимология:** "Hash" (англ.) — "рубить, крошить, мелко резать". Кулинарный термин: блюдо из мелко нарезанного мяса. Хеш-функция "рубит" входные данные, перемешивает, выдаёт результат, в котором исходная структура неузнаваема.

**Идея:**
1. Превратить произвольный ключ в число (хеш-функция)
2. Использовать как индекс в массиве
3. Получить O(1) в среднем

### Хеш-функция: требования

1. **Детерминированность:** один вход → всегда один и тот же выход
2. **Равномерность:** выходы распределены равномерно
3. **Быстрота:** O(длина ключа) или лучше

**НЕ требуется:** криптостойкость, уникальность выхода.

### Пример хеш-функции для строки

**Плохая (не учитывает порядок):**

```ruby
def bad_hash(str)
  str.chars.sum(&:ord)  # "ab" и "ba" дадут одинаковый хеш
end
```

**Хорошая (классическая, как в Java):**

```ruby
def good_hash(str)
  hash = 0
  str.each_char { |c| hash = hash * 31 + c.ord }
  hash
end
```

Умножение на 31 делает хеш зависимым от порядка символов.

```
"ab": (0*31 + 97)*31 + 98 = 3105
"ba": (0*31 + 98)*31 + 97 = 3135
```

### От хеша к индексу

```ruby
index = hash(key) % capacity
```

Если `capacity = 16` и `hash("alice") = 3105`, то `index = 3105 % 16 = 1`.

### Коллизия

Данных бесконечно много, индексов — ограниченное количество. Разные ключи могут получить одинаковый индекс. Это **коллизия** (столкновение).

### Стратегия 1: Chaining (цепочки)

**Этимология:** "Chain" — цепь. Элементы с одинаковым индексом связаны в цепочку (список).

Каждая ячейка хранит список элементов с одинаковым индексом.

```
┌───┬─────────────────────────────────────┐
│ 0 │ [("bob", data)]                     │
│ 1 │ [("alice", data) → ("charlie", data)]│  ← коллизия
│ 2 │ []                                  │
└───┴─────────────────────────────────────┘
```

**get(key):** вычисляем индекс, идём по списку, сравниваем ключи.

**put(key, value):** вычисляем индекс, ищем в списке, обновляем или добавляем.

### Стратегия 2: Open Addressing (открытая адресация)

**Этимология:** "Open" — весь массив "открыт" для размещения любого элемента, не только того, чей хеш указывает на эту ячейку.

Все элементы живут прямо в массиве. Если ячейка занята — ищем следующую свободную.

**Linear Probing:** если ячейка `i` занята, пробуем `i+1`, `i+2`, ...

```
put("alice"), hash % 8 = 1 → кладём в 1
put("charlie"), hash % 8 = 1 → занято, пробуем 2, кладём
put("dave"), hash % 8 = 2 → занято, пробуем 3, кладём
```

**Проблема: кластеризация.** Группы занятых ячеек растут, поиск замедляется.

### Tombstones (надгробия)

**Этимология:** "Tombstone" — надгробие. Маркер на месте "умершего" (удалённого) элемента, показывающий что здесь что-то было.

При удалении в open addressing нельзя просто поставить nil — это сломает поиск элементов, которые "перепрыгнули" эту ячейку.

**Решение:** ставим специальный маркер tombstone. При поиске — "продолжай искать", при вставке — "можно занять".

### Load Factor (коэффициент заполнения)

**Этимология:** "Load" — нагрузка, заполненность. Показывает насколько "нагружена" таблица.

```
load_factor = n / capacity
```

Где `n` — количество элементов.

**Chaining:** может быть > 1 (списки любой длины).

**Open addressing:** должен быть < 1 (физически нельзя разместить больше элементов, чем ячеек). На практике держат ≤ 0.7.

### Rehashing (перехеширование)

**Этимология:** "Re-" — заново, "hashing" — хеширование. Пересчитываем хеши для новой таблицы.

Когда load factor превышает порог (обычно 0.75):

1. Выделяем новый массив большего размера (×2)
2. Перехешируем все элементы (hash % новый_capacity ≠ hash % старый_capacity)
3. Освобождаем старый массив

Стоит O(n), но происходит редко → амортизированно O(1) на операцию.

### Сложность операций

| Операция | В среднем | В худшем |
|----------|-----------|----------|
| get | O(1) | O(n) |
| put | O(1)* | O(n) |
| delete | O(1) | O(n) |

`*` — амортизированно с учётом rehashing

**Худший случай:** все ключи в одной ячейке (плохая хеш-функция или атака).

### Сравнение стратегий

| Аспект | Chaining | Open Addressing |
|--------|----------|-----------------|
| Память | Указатели на списки | Только массив |
| Локальность | Хуже | Лучше |
| Load factor | Может быть > 1 | Должен быть < 1 |
| Удаление | Простое | Нужны tombstones |
| Код | Проще | Сложнее |

### Проблема: нет порядка для вытеснения

O(1) доступ по ключу — есть. Но хеш-таблица не сохраняет порядок вставки и не знает, какой элемент "старый", а какой "свежий".

Для кэша с ограниченным размером нужно вытеснять элементы. Какие? Нужна политика вытеснения — и структура, которая её поддерживает.

---

## Задача: ограниченный кэш с вытеснением

Хотим хранить ограниченное количество элементов с O(1) доступом по ключу. При переполнении — автоматически вытеснять "ненужные" элементы.

Какой элемент считать ненужным? Простая эвристика: если к элементу давно не обращались, он скорее всего не понадобится. Это политика **LRU** — Least Recently Used.

### ADT: Cache

Словарь с ограниченной ёмкостью и автоматическим вытеснением:

| Операция | Сигнатура | Сложность |
|----------|-----------|-----------|
| Получить значение | `get(key) → value` | O(1) |
| Записать значение | `put(key, value)` | O(1) |

**Инвариант:** при превышении capacity автоматически вытесняется элемент по заданной политике (LRU/LFU/etc).

### Реализация: LRU-кэш

**LRU** — Least Recently Used, "наименее недавно использованный".

Кэш с ограниченной ёмкостью. При переполнении вытесняет элемент, к которому дольше всего не обращались.

**Операции:**
- `get(key)` — вернуть значение. Если есть — элемент становится "свежим". Если нет — nil.
- `put(key, value)` — записать. Элемент становится "свежим". При переполнении — вытеснить самый старый.

### Пример работы (capacity = 2)

```
put("a", 1) → [a]
put("b", 2) → [b, a]
get("a")    → [a, b]        a стал свежим
put("c", 3) → [c, a]        b вытеснен (был tail)
get("b")    → nil           b уже нет в кэше
```

### Почему нужны две структуры

**Только хеш-таблица:** O(1) доступ по ключу, но как отслеживать порядок использования?

**Только двусвязный список:** O(1) перемещение в начало, но как найти элемент по ключу за O(1)?

**Решение:** комбинация.

### Структура

```
HashMap: key → Node

Doubly Linked List: head (свежий) ⇄ ... ⇄ tail (старый)

Узел хранит: key, value, prev, next
```

**Почему ключ в узле?** При вытеснении берём tail, нужно удалить из хеша по ключу.

### Класс узла для LRU

```ruby
class Node
  attr_accessor :key, :value, :prev, :next

  def initialize(key, value)
    @key = key
    @value = value
    @prev = nil
    @next = nil
  end
end
```

### Вспомогательные методы

```ruby
# Отцепить узел из текущего места
def remove_node(node)
  if node.prev
    node.prev.next = node.next
  else
    @head = node.next
  end

  if node.next
    node.next.prev = node.prev
  else
    @tail = node.prev
  end
end

# Вставить в начало (сделать свежим)
def add_to_head(node)
  node.prev = nil
  node.next = @head

  if @head
    @head.prev = node
  else
    @tail = node
  end

  @head = node
end
```

### Операции

```ruby
def get(key)
  node = @hash[key]
  return nil unless node
  return node.value if node == @head  # уже свежий

  remove_node(node)
  add_to_head(node)
  node.value
end

def put(key, value)
  if @hash.key?(key)
    node = @hash[key]
    node.value = value
    remove_node(node)
    add_to_head(node)
  else
    node = Node.new(key, value)
    @hash[key] = node
    add_to_head(node)

    if @hash.size > @capacity
      # Важен порядок! Сначала удаляем из хеша, потом из списка
      @hash.delete(@tail.key)
      remove_node(@tail)
    end
  end
end
```

### Критичный баг с порядком операций

```ruby
# НЕПРАВИЛЬНО:
remove_node(@tail)         # @tail обновляется на предпоследний!
@hash.delete(@tail.key)    # удаляем ключ НОВОГО tail!

# ПРАВИЛЬНО:
@hash.delete(@tail.key)    # сначала берём ключ старого tail
remove_node(@tail)         # потом удаляем узел
```

### Сложность

| Операция | Время | Почему |
|----------|-------|--------|
| get | O(1) | Хеш O(1) + манипуляции O(1) |
| put | O(1) | Хеш O(1) + манипуляции O(1) |

### Альтернатива: LFU

**LFU** — Least Frequently Used, "наименее часто используемый". Вытесняет элемент с наименьшим количеством обращений за всё время.

**Отличие от LRU:**
- LRU смотрит на **время** последнего обращения
- LFU смотрит на **частоту** обращений

**Проблема LFU — cache pollution (загрязнение кэша):**

```
T=0-100: страница A читается 1000 раз, count=1000
T=101:   страница A больше никому не нужна
T=102:   страница B нужна, count=1
T=103:   нужно вытеснить кого-то

LFU выберет B (count=1), хотя A уже бесполезна.
```

Элемент, популярный в прошлом, "застревает" в кэше из-за высокого счётчика. Он вытесняет новые элементы, которые могли бы быть полезны.

**Когда LFU лучше:** стабильные паттерны доступа, чёткое разделение на "горячие" и "холодные" данные.

**Когда LRU лучше:** меняющиеся паттерны доступа, недавнее обращение — хороший предиктор будущего.

### Проблема: contention в многопоточной среде

LRU работает идеально по сложности, но при каждом обращении нужно переместить элемент в начало списка. Это 4 записи в указатели:

1. `node.prev.next = node.next`
2. `node.next.prev = node.prev`
3. `head.prev = node`
4. `node.next = head`

Эти изменения должны быть согласованы — иначе список развалится. На практике это означает синхронизацию вокруг структуры (мьютекс/спинлок) или более сложные неблокирующие реализации.

```
Поток 1: хочет переместить элемент A → ждёт блокировку
Поток 2: хочет переместить элемент B → ждёт блокировку
...
1000 потоков ждут одну блокировку → bottleneck
```

В однопоточной программе это не проблема. Но сервер базы данных с тысячами параллельных запросов к кэшу — это постоянное ожидание на блокировке.

---

## Задача: кэш без блокировок при каждом обращении

Хотим кэш с вытеснением, но без contention при чтении. Готовы пожертвовать точностью вытеснения ради масштабируемости.

### ADT: ApproximateCache

Кэш с приблизительной политикой вытеснения:

| Операция | Сигнатура | Сложность | Contention |
|----------|-----------|-----------|------------|
| Получить значение | `get(key) → value` | O(1) | Нет* |
| Записать значение | `put(key, value)` | O(1) | При вытеснении |

`*` — атомарный инкремент вместо манипуляций со списком

**Компромисс:** менее точное вытеснение в обмен на масштабируемость.

### Реализация: Clock-Sweep

**Этимология:**
- "Clock" — часы. Указатель движется по кругу, как стрелка.
- "Sweep" — подметать. Стрелка "сметает" единицы со счётчиков.

**Альтернативное название:** Second-Chance Algorithm — элемент с ненулевым счётчиком получает "второй шанс".

**Идея:** откажемся от точного порядка. Вместо этого:

1. Каждый элемент хранит `usage_count`
2. При обращении: инкрементируем счётчик (атомарно, без блокировок общих структур)
3. При вытеснении: стрелка идёт по кругу, уменьшает счётчики, вытесняет первый с нулевым

```
        [buf 0]
           ↑
    [buf 7]   [buf 1]

  [buf 6]   ↻   [buf 2]    ← стрелка движется по кругу

    [buf 5]   [buf 3]

        [buf 4]
```

### Алгоритм

**При обращении (cache hit):**

```ruby
element.usage_count += 1  # атомарный инкремент
element.usage_count = [element.usage_count, MAX_COUNT].min
```

Никаких блокировок общих структур — только запись в своё поле.

**При поиске жертвы (cache miss):**

```ruby
def find_victim
  loop do
    element = buffer[@clock_hand]

    if element.usage_count == 0
      return element                    # нашли жертву
    else
      element.usage_count -= 1          # даём "второй шанс"
      @clock_hand = (@clock_hand + 1) % capacity
    end
  end
end
```

### Пример timeline

```
T=0: элемент загружен,      usage_count = 1
T=1: обращение,             usage_count = 2
T=2: обращение,             usage_count = 3
T=3: стрелка прошла,        usage_count = 2
T=4: стрелка прошла,        usage_count = 1
T=5: обращение,             usage_count = 2
T=6: стрелка прошла,        usage_count = 1
T=7: стрелка прошла,        usage_count = 0 → кандидат на вытеснение
```

### Почему лимит счётчика — маленькое число

PostgreSQL использует `BM_MAX_USAGE_COUNT = 5` для буферного кэша.

Без лимита элемент с `usage_count = 1000` требует 1000 проходов стрелки для вытеснения. Даже если он уже не нужен — "застревает" (та же проблема cache pollution, что у LFU).

С лимитом 5: максимум 5 проходов → быстрая адаптация к изменению паттерна доступа.

### Где теряется точность

```
Два элемента, оба usage_count = 1:

Элемент A: последнее обращение 10 секунд назад
Элемент B: последнее обращение 1 секунду назад

LRU знает: вытеснить A
Clock-sweep: видит только счётчики, выберет того, до кого дойдёт первой
```

Clock-sweep не различает элементы с одинаковым счётчиком. Это приемлемая потеря точности в обмен на масштабируемость.

### Сравнение подходов

| Аспект | LRU | LFU | Clock-Sweep |
|--------|-----|-----|-------------|
| При обращении | 4 записи + блокировка | Обновление счётчика | Атомарный инкремент |
| Точность | Точный порядок по времени | Точный счётчик за всё время | Приблизительный |
| Cache pollution | Нет | Да | Нет (затухание) |
| Contention | При каждом обращении | При каждом обращении | Только при вытеснении |
| Масштабируемость | Хуже | Хуже | Лучше |

Clock-sweep добавляет **затухание**: счётчик уменьшается при проходах стрелки. Прошлая популярность не защищает навечно — если элемент перестал использоваться, его счётчик постепенно дойдёт до нуля.

### Сложность

| Операция | LRU | Clock-Sweep |
|----------|-----|-------------|
| Обращение | O(1) + блокировка | O(1) атомарный инкремент |
| Вытеснение | O(1) | O(n) в худшем* |

`*` — на практике при нормальной нагрузке обходит несколько элементов.

### Где используется

- PostgreSQL — shared buffers
- Операционные системы — page replacement
- Другие СУБД и кэши с высокой конкурентностью

### Классификация алгоритмов вытеснения

```
              Алгоритмы вытеснения
                      │
       ┌──────────────┼──────────────┐
       │              │              │
      LRU            LFU          Random
       │
       ├── Точный LRU (список + хеш)
       │   └── Проблема: contention
       │
       └── Аппроксимации LRU
           ├── Clock-Sweep (PostgreSQL)
           ├── CLOCK (оригинальный)
           └── ARC, 2Q, LIRS...
```

---

## Сводные таблицы

### Массив vs Связный список

| Операция | Массив | Односвязный список | Двусвязный список |
|----------|--------|-------------------|-------------------|
| Доступ по индексу | O(1) | O(n) | O(n) |
| Поиск по значению | O(n) | O(n) | O(n) |
| Вставка в начало | O(n) | O(1) | O(1) |
| Вставка в конец | O(1)* | O(1)** | O(1)** |
| Вставка в середину | O(n) | O(1)*** | O(1)*** |
| Удаление из начала | O(n) | O(1) | O(1) |
| Удаление из конца | O(1) | O(n) | O(1)** |
| Удаление из середины | O(n) | O(1)*** | O(1)*** |

`*` — амортизировано
`**` — при наличии tail указателя
`***` — если уже есть указатель на место; иначе +O(n) на поиск

### ADT: Стек, Очередь, Дек

| ADT | Порядок | Операции | Лучшая реализация |
|-----|---------|----------|-------------------|
| Стек | LIFO | push/pop с одного конца | Динамический массив |
| Очередь | FIFO | enqueue в конец, dequeue из начала | Ring buffer |
| Дек | Оба | Все операции с обоих концов | Ring buffer или двусвязный список |

### Хеш-таблица

| Операция | В среднем | В худшем |
|----------|-----------|----------|
| get | O(1) | O(n) |
| put | O(1)* | O(n) |
| delete | O(1) | O(n) |

`*` — амортизировано

### LRU-кэш

| Операция | Время | Структуры |
|----------|-------|-----------|
| get | O(1) | Хеш-таблица + двусвязный список |
| put | O(1) | Хеш-таблица + двусвязный список |

### LFU vs LRU

| Аспект | LRU | LFU |
|--------|-----|-----|
| Критерий вытеснения | Время последнего обращения | Количество обращений |
| Cache pollution | Нет | Да (старые популярные элементы застревают) |
| Адаптация к изменениям | Быстрая | Медленная |
| Лучше для | Меняющиеся паттерны доступа | Стабильные паттерны доступа |

### Clock-Sweep vs LRU vs LFU

| Аспект | LRU | LFU | Clock-Sweep |
|--------|-----|-----|-------------|
| Критерий | Время | Частота | Частота + затухание |
| Точность | Точный порядок | Точный счётчик | Приблизительный |
| Cache pollution | Нет | Да | Нет (затухание) |
| Contention | При каждом обращении | При каждом обращении | Только при вытеснении |
| Масштабируемость | Хуже | Хуже | Лучше |

---

## Sources

- OpenJDK (например, JDK 21): `java.util.ArrayList` (`grow`). <https://github.com/openjdk/jdk/blob/jdk21u/src/java.base/share/classes/java/util/ArrayList.java>
- CPython (например, Python 3.12): over-allocation списка. <https://github.com/python/cpython/blob/v3.12.0/Objects/listobject.c>
- Go (например, Go 1.22): рост `slice`. <https://github.com/golang/go/blob/go1.22.0/src/runtime/slice.go>
- PostgreSQL (например, 16): clock-sweep / `BM_MAX_USAGE_COUNT`. <https://github.com/postgres/postgres/blob/REL_16_0/src/include/storage/buf_internals.h> и <https://github.com/postgres/postgres/blob/REL_16_0/src/backend/storage/buffer/freelist.c>
