# Дерево

**Предпосылки:** оценка сложности в O(…), [граф](00-graph.md) (вершины, рёбра, путь, цикл, связность).

Граф допускает произвольные связи, в том числе циклы и несколько путей между вершинами. Если ограничить его до связного ациклического — между любыми двумя вершинами останется ровно один путь — получится **дерево**.

## Определение

Дерево — это **связный ациклический граф**: из любой вершины можно добраться до любой другой, и при этом нет циклов.

Ключевое свойство: в дереве **количество рёбер = количество вершин − 1** (E = V − 1). Добавишь ещё одно ребро — появится цикл. Уберёшь одно — граф распадётся. На практике это даёт быструю проверку: если в связном графе E = V − 1, это дерево; если E ≥ V — есть циклы.

Название "дерево" происходит от внешнего вида: если нарисовать такой граф и "подвесить" за одну вершину, он напоминает перевёрнутое дерево с ветвями.

"Просто дерево" (свободное дерево, free tree) — граф без выделенной вершины. **Корневое дерево** — с выделенным **корнем** (root). Корень задаёт направление: от корня к листьям. После этого у каждого узла появляются роли "родитель" и "ребёнок".

## Терминология корневого дерева

**Родитель** (parent) — вершина на уровень выше. **Ребёнок** (child) — вершина на уровень ниже. **Лист** (leaf) — вершина без детей. **Внутренняя вершина** — вершина с детьми. **Глубина узла** — длина пути от корня до узла. **Высота дерева** — длина пути от корня до самого глубокого листа.

## Хранение корневого дерева

Для работы с деревом нужно представить связи "родитель — ребёнок" в памяти. Корневое дерево можно хранить через список детей:

```ruby
class TreeNode
  attr_accessor :value, :children

  def initialize(value)
    @value = value
    @children = []
  end
end
```

Или через указатель на родителя:

```ruby
parent = {
  А: nil,   # корень
  Б: :А,
  В: :А,
  Г: :В
}
```

Список детей удобен, когда нужно обходить дерево сверху вниз (от корня к листьям) — например, рендеринг DOM, обход файловой системы. Указатель на родителя удобен, когда нужно подниматься вверх — например, найти путь от узла к корню (хлебные крошки в навигации, `git log --graph` поднимается от коммита к предкам).

## Операции

Выбор хранения определяет, какие операции эффективны. Основные операции корневого дерева: добавить ребёнка к узлу, удалить поддерево, обойти все узлы. Добавление ребёнка — O(1) (дописать в список children). Удаление поддерева — O(1) если есть указатель на узел (отцепить от родителя), но O(k) если нужно освободить память k удалённых узлов. Обход всех узлов — O(n). Поиск по значению — O(n), потому что без дополнительных ограничений на структуру нет способа сузить область поиска.

Если ограничить число детей до двух, у каждого узла появляются фиксированные роли: левый и правый ребёнок. Это **бинарное дерево**: [Бинарное дерево](02-binary-tree.md).

## Sources

- Cormen, Leiserson, Rivest, Stein. *Introduction to Algorithms* (CLRS), 4th ed. (trees as connected acyclic graphs, свойства E = V − 1).
