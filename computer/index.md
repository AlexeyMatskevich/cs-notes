# Аппаратное обеспечение

**Предпосылки:** базовое программирование (переменные, циклы, функции).

Программы работают с данными — читают, записывают, передают. Но данные лежат на разных расстояниях от процессора: одни прямо внутри него, другие в оперативной памяти, третьи на диске. Каждое расстояние — это разница в скорости доступа, от долей наносекунды до десятков миллисекунд. Между самым быстрым и самым медленным хранилищем — семь порядков. Аппаратная архитектура компьютера определяется тем, как каждый уровень прячет задержку предыдущего и какую цену за это платит.

## Порядок изучения

Заметки выстроены по пути данных: от процессора, где инструкции выполняются, через уровни памяти — к устройствам хранения и механизму их взаимодействия.

- [Процессор](00-cpu.md) — как инструкции выполняются и почему частота не определяет скорость
- [Иерархия памяти и кеш](01-memory-hierarchy.md) — почему процессор не может просто читать из RAM и как промежуточные буферы решают проблему
- [Когерентность кешей](02-cache-coherency.md) — как несколько ядер видят одинаковые данные и сколько это стоит
- [Оперативная память](03-ram.md) — почему последовательный доступ на порядок быстрее случайного и откуда берутся ~100 нс задержки
- [Хранилище](04-storage.md) — энергонезависимость: цена персистентности в микросекундах и миллисекундах
- [Шины и DMA](05-buses-and-dma.md) — как данные перемещаются между процессором, памятью и устройствами
- [ABI и размещение данных](06-abi-and-data-layout.md) — как компилятор, ОС и процессор договариваются о расположении данных

## Как всё связано

**Скорость vs объём:** каждый уровень иерархии на порядок медленнее, но на порядок больше. Ячейки внутри процессора — 128 байт за доли наносекунды. Оперативная память — десятки гигабайт за ~100 нс. Диск — терабайты за миллисекунды. Выбор уровня определяет, сколько данных доступно и какой ценой.

**Латентность vs пропускная способность:** последовательный доступ упирается в пропускную способность канала, случайный — в задержку каждого отдельного обращения. Это верно на каждом уровне — от оперативной памяти до диска. Порядок обхода данных часто важнее абсолютной скорости устройства.

**Параллелизм vs согласованность:** несколько ядер ускоряют вычисления, но требуют аппаратного протокола, который гарантирует что все ядра видят одинаковые данные. Когда логически независимые данные оказываются физически рядом, протокол согласования замедляет работу в десятки раз без видимой причины.

**Персистентность vs скорость:** энергонезависимое хранилище на 3–5 порядков медленнее оперативной памяти. Контроллер SSD прячет асимметрию между чтением и записью, но порождает дополнительную внутреннюю запись и непредсказуемые задержки при внутренней уборке.

**Простота vs производительность:** простейший способ передать данные от устройства в память — процессор копирует каждый байт сам. Это просто, но процессор занят только копированием. Механизм прямого доступа к памяти сложнее, но освобождает процессор для полезной работы.

**Скорость вызова vs универсальность:** передача аргументов функции через ячейки внутри процессора в разы быстрее, чем через стек в памяти. Но ячеек мало, а стек масштабируется на любую глубину вложенности. Двоичный интерфейс фиксирует компромисс: первые аргументы — в быстрых ячейках, остальные — на стеке.

## См. также

- [Linux](../linux/index.md) — операционная система, которая управляет этим оборудованием
- [PostgreSQL: буферный кеш](../databases/postgresql/durability/01-buffer-cache.md) — shared_buffers и page cache как прикладной пример иерархии памяти
- [PostgreSQL: WAL](../databases/postgresql/durability/00-wal.md) — sequential I/O как сознательный выбор ради производительности
- [System Design: кеширование](../system-design/07-caching.md) — иерархия латентностей на уровне распределённых систем

## Sources

- John L. Hennessy, David A. Patterson, 2017, *Computer Architecture: A Quantitative Approach* — 6th edition: https://www.elsevier.com/books/computer-architecture/hennessy/978-0-12-811905-1
- Jacob, Ng, Wang, 2007, *Memory Systems: Cache, DRAM, Disk* — Morgan Kaufmann
- Intel, 2024, *Intel 64 and IA-32 Architectures Software Developer's Manual*: https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html
